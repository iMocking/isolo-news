// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"isolo-news/server/internal/repository/ent/userquest"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserQuest is the model entity for the UserQuest schema.
type UserQuest struct {
	config `json:"-"`
	// ID of the ent.
	// ULID 主键
	ID string `json:"id,omitempty"`
	// 创建时间（UTC Unix 时间戳）
	CreatedAt int64 `json:"created_at,omitempty"`
	// 更新时间（UTC Unix 时间戳）
	UpdatedAt int64 `json:"updated_at,omitempty"`
	// 用户 ID
	UserID string `json:"user_id,omitempty"`
	// 任务 ID
	QuestID string `json:"quest_id,omitempty"`
	// 当前进度
	Progress int `json:"progress,omitempty"`
	// 状态：not_started / in_progress / completed
	Status string `json:"status,omitempty"`
	// 任务日期（UTC 日期时间戳）
	DateAt       int64 `json:"date_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserQuest) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userquest.FieldCreatedAt, userquest.FieldUpdatedAt, userquest.FieldProgress, userquest.FieldDateAt:
			values[i] = new(sql.NullInt64)
		case userquest.FieldID, userquest.FieldUserID, userquest.FieldQuestID, userquest.FieldStatus:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserQuest fields.
func (_m *UserQuest) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userquest.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case userquest.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Int64
			}
		case userquest.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Int64
			}
		case userquest.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case userquest.FieldQuestID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field quest_id", values[i])
			} else if value.Valid {
				_m.QuestID = value.String
			}
		case userquest.FieldProgress:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field progress", values[i])
			} else if value.Valid {
				_m.Progress = int(value.Int64)
			}
		case userquest.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case userquest.FieldDateAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field date_at", values[i])
			} else if value.Valid {
				_m.DateAt = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserQuest.
// This includes values selected through modifiers, order, etc.
func (_m *UserQuest) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this UserQuest.
// Note that you need to call UserQuest.Unwrap() before calling this method if this UserQuest
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserQuest) Update() *UserQuestUpdateOne {
	return NewUserQuestClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserQuest entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserQuest) Unwrap() *UserQuest {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserQuest is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserQuest) String() string {
	var builder strings.Builder
	builder.WriteString("UserQuest(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.CreatedAt))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.UpdatedAt))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	builder.WriteString("quest_id=")
	builder.WriteString(_m.QuestID)
	builder.WriteString(", ")
	builder.WriteString("progress=")
	builder.WriteString(fmt.Sprintf("%v", _m.Progress))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("date_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.DateAt))
	builder.WriteByte(')')
	return builder.String()
}

// UserQuests is a parsable slice of UserQuest.
type UserQuests []*UserQuest
